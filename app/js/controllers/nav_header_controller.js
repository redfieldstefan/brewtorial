'use strict';

module.exports = function(app) {

  NavHeaderController.$inject = ['$scope', '$http'];

  function NavHeaderController($scope, $http) {
    
    function init() {
      
    }

    init();
    
  };

  app.controller('NavHeaderController', NavHeaderController);

};
